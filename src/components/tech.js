import React from "react"
import styled from "@emotion/styled"
import Typer from "../components/typer"

const Wrapper = styled.section`
  height: 100vh;
  width: 100vw;
  position: relative;
`

const Tech = () => {
  return <Wrapper id="tech"> Test</Wrapper>
}

export default Tech
